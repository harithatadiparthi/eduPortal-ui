import { FirstnamefilterPipe } from './firstnamefilter.pipe';

describe('FirstnamefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstnamefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
