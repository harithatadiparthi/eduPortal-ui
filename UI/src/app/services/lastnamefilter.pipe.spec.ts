import { LastnamefilterPipe } from './lastnamefilter.pipe';

describe('LastnamefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LastnamefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
