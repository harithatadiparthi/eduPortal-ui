import { ZipcodefilterPipe } from './zipcodefilter.pipe';

describe('ZipcodefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ZipcodefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
